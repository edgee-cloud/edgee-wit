package edgee:protocols;

interface consent-mapping {
    type settings = list<tuple<string,string>>;

    enum consent {
        pending,
        granted,
        denied,
    }

    map: func(cookie: string, settings: settings) -> option<consent>;
}